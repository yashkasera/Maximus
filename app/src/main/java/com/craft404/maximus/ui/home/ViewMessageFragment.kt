package com.craft404.maximus.ui.home

import androidx.fragment.app.Fragment

class ViewMessageFragment : Fragment() {
    companion object {
        private const val TAG = "ViewMessageFragment"
        fun newInstance(): ViewMessageFragment {
            val fragment = ViewMessageFragment()
            return fragment
        }
    }
}